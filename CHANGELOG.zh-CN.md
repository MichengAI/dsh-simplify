# 变更记录

[English](CHANGELOG.md)

## 0.1.0 - 2026-09-06

- 新增 `/simplify`：当前改动、暂存区、指定提交和显式文件或目录范围。
- 新增中文编辑任务提示词，明确范围、行为、快照与验证要求。
- 完善简化检查方向、行为等价判断、项目规范读取、并发编辑保护及验证报告，避免过度抽象、越界修改和无依据的优化。
- 使用 DSH subprocess 参数数组及 NUL 分隔解析，支持中文、空格路径和未跟踪文件。
- Git 错误、取消、超时及截断时阻止入队；提供文件数、内容大小和提示词上限。
- 增加暂存区一致性保护、HEAD/index/内容复核及仅默认模式的上一提交回看。
- 新增真实 Git 回归测试和可选真实 DSH 服务集成测试。
- 按补丁正文核对新旧行数，排除上下文行并拒绝残缺块。
- 初始化公开仓库及双语文档，项目采用 Apache-2.0。

### 已知问题

- 部分宿主界面未明显显示“没有可简化的当前代码行”的命令结果。后端已返回结果且不会发起模型请求，反馈可见性仍待排查。
