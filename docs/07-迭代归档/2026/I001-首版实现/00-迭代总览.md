# I001 首版实现

状态：已完成
负责人：Codex（实现与验证）；用户（产品决策）
优先级：P1
更新时间：2026-09-06 18:18（Asia/Shanghai）

## 目标与边界

实现自己的 DSH 代码简化插件。参考三个移植项目和 `pi-simplify@0.2.3`，保留四条简化原则，修复 Git 失败、路径、未跟踪文件及暂存区错位问题。仅本地实现，不发布或修改现有 profile。

## 任务

- 已完成：严格 TypeScript 工程、包元数据、DSH bundle。
- 已完成：Git argv 执行、取消、超时、截断检查和文件大小限制。
- 已完成：NUL 文件解析、范围选择、默认回退、初始仓库和暂存区一致性保护。
- 已完成：内容与 index 快照复核、中文提示词、命令注册及卸载处理。
- 已完成：首批回归测试及真实 DSH 服务隔离集成。
- 已完成：27 项测试、包内容检查、依赖审计和最终文档校验。

## 验收

- [x] 关键测试在实现前确认失败，最小实现后通过。
- [x] Git 错误和不完整输出不产生模型任务。
- [x] 中文、空格、未跟踪文件和默认回退行为经真实 Git 验证。
- [x] 暂存区与工作区不一致时阻止错位审查。
- [x] 真实 DSH 命令服务、subprocess、消息投递及卸载通过。
- [x] 最终检查、打包与文档链接校验；详见验收记录。

## 阻塞与待处理

当前无实现阻塞。npm 发布、GitHub 建库及真实用户 profile 安装未执行，不作为本次验收前提。

遗留清理：本轮自动审批拒绝删除测试临时目录；由后续执行者在工具允许时清理 `.test-tmp`，承接于当前状态。临时目录不进入安装包。

## 恢复与提交

本轮无用户数据变更；插件没有执行模型修改。尚未添加到运行中的 profile，无需回退部署。
关联提交：未提交（当前目录非 Git 仓库）。

## 阅读顺序

1. [README](../../../../README.md)
2. 本总览
3. [验收记录](01-验收记录.md)
4. [用户授权后的本地部署记录](02-本地部署记录.md)
