# 本地部署记录

更新时间：2026-09-06 18:44（Asia/Shanghai）

用户在首版交付后明确要求安装到本地供测试。本记录承接首版实现，不改变此前验收记录的历史事实。

- 安装对象：`@michengai/dsh-simplify@0.1.0`，本项目生成的 `michengai-dsh-simplify-0.1.0.tgz`。
- 目标：DSH Codex Desktop 使用的 `%USERPROFILE%\.dsh\profiles\web`。
- 执行：`dsh plugin --profile web add` 加本地 tgz 绝对路径，退出码 0，仅新增本插件。
- 验证：profile dependencies 和 bundles 均包含本插件；实际安装版本 `0.1.0`；`--dump-config` 包含 `michengai-simplify`。
- 激活：桌面检测清单变化，自动更换后端进程；启动诊断 `stage=healthy`，时间 `2026-09-06T10:44:19.845Z`。
- 依赖：安装器提示四个 peer 缺失，但从已安装插件目录实际解析到宿主 Cordis `4.0.2` 及 commands、llm、subprocess `0.1.2-rc.1`，全部满足声明；没有重复安装宿主包。
- 未执行 `/simplify`，由用户在有 Git 改动的项目会话中测试，未触发模型编辑。
- 浏览器辅助检查因客户端阻止打开本地端口未完成；激活结论依据安装清单、组合配置及桌面健康诊断。

## 恢复

安装前已备份 profile 的 `package.json`、`pnpm-lock.yaml`、`pnpm-workspace.yaml` 至 `%LOCALAPPDATA%\dsh-simplify-install-backup-20260906-184414`。

需要卸载时运行 `dsh plugin --profile web remove @michengai/dsh-simplify`，桌面会按清单变化重新加载。备份只用于排障时核对本次安装差异，不应覆盖后续其他插件的改动。卸载不会撤销 Agent 后续产生的代码修改。
