# 验收记录

更新时间：2026-09-06 19:16（Asia/Shanghai）

## 实现与许可

- 早期本地预览的改编提示词已替换为本项目的任务清单、编辑决策及验证流程。
- 补丁解析改为按正文逐行推进游标、分别核对旧侧和新侧计数，只收集实际新增行；上下文不授权编辑，残缺或超量正文报错。
- 项目使用 Apache-2.0，LICENSE、包元数据、锁文件及双语 README 一致；不再分发早期改编部分及对应许可附件。
- 未新增运行依赖；已有依赖的许可元数据按原样保留。

## 验证结果

| 检查 | 结果 |
| --- | --- |
| 测试先行 | 旧实现误收上下文行、接受残缺正文的两个回归场景先失败；重写后通过 |
| `npm run check` | 类型检查、构建、31 项测试全部通过，无跳过 |
| 补丁边界 | 覆盖上下文排除、旧新行计数不符、零行侧、纯删除、无末尾换行、多块定位及加号正文 |
| 真实 Git / DSH | 原有 17 项 Git 回归和 1 项真实 DSH 服务隔离集成通过；测试不请求模型 |
| `npm pack --dry-run --ignore-scripts --json` | 基于本轮已构建产物检查，双语 README/CHANGELOG、LICENSE、入口及类型声明在包内；无旧许可附件、测试夹具或安装备份 |
| 文档与元数据 | 已有相对链接可解析，UTF-8 无替换字符，根包与锁文件的许可、依赖及引擎声明一致 |
| 提交前内容检查 | 凭据及本机用户名路径模式扫描无命中；依赖、构建输出、tgz 和测试临时目录被忽略 |

## 交付边界

GitHub 建库与推送结果归迭代总览。没有执行 npm 发布或创建 GitHub Release。没有覆盖已安装的本机旧预览包，也没有验证真实模型编辑；命令反馈可见性仍待排查，见 [待办与阻塞](../../00-交接入口/03-待办与阻塞.md)。
